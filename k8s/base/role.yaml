apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: kubesense-backend-role
  namespace: kubesense

rules:
  - apiGroups: [""]
    resources:
      - pods
      - pods/log
      - events
    verbs:
      - get
      - list
      - watch