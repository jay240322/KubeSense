apiVersion: v1
kind: Service

metadata:
  name: kubesense-frontend-service
  namespace: kubesense

spec:
  selector:
    app: kubesense-frontend

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

  type: ClusterIP